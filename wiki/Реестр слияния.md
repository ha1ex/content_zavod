# Реестр слияния `main ← integration` (28.08.2026)

Коммит `a6096d7`, родители `1084285` (main) и `04c9c57` (integration).
75 файлов, +5501 / −133. Ветки разошлись 07.08 на общем предке `6f2113c`:
68 коммитов на `main`, 6 на `integration`.

Конфликтов: **23 файла, 39 блоков**. Ниже — что выбрано, что при этом
отброшено и что из-за этого стоит перепроверить глазами.

---

## 1. Решения по конфликтам

### Шрифт и типографика — сторона `main`

| файл | блоков | отброшено |
|---|---|---|
| `packages/ui/src/tokens.css` | 1 | `var(--font-roboto)` в токене: на `main` его подставляет `globals.css` |
| `design-system/kaiten-v01/tokens.json` | 1 | то же |
| `wiki/design-system/typography.md` | 1 | то же |
| `mocks/CliAiModelsMock.tsx` | 1 | `Inter` в стеке |
| `mocks/CliProjectGanttMock.tsx` | 1 | `Inter` в стеке |
| `mocks/CliTerminalFinalAnimatedMock.tsx` | 1 | `Inter` в стеке |
| `mocks/CliTerminalHeroAnimatedMock.tsx` | 1 | `Inter` в стеке |

Основание — коммит `d3304bd` «fix(ds): Roboto через next/font, Inter убран,
трекинг обнулён». Это оформленное решение дизайн-системы, отражённое в
`typography.md`. Сторона `integration` тянула старый стек с `Inter`.

**Риск:** низкий. Проверить нечего — `Inter` нигде не установлен как рабочий шрифт.

### `apps/web/app/layout.tsx` — собран вручную

Комментарий `main` + веса `['400','500','600','700']` со стороны `integration`.

**Основание:** на `main` в списке весов не было 700, а `font-bold` используется
в 16 файлах `packages/ui/src/landing` — без загруженного начертания браузер
синтезирует жирный.

**Проверить:** насыщенность жирного текста в моках (`FeatureMocksV01`,
`CliTerminalHeroAnimatedMock`).

### Отступы секций и шапки — сторона `main`

| файл | блоков | отброшено |
|---|---|---|
| `landing/MediaCopy.tsx` | 1 | `pb` по `titleSize`, `flushTop` в отступах |
| `landing/TabbedFeatureSection.tsx` | 1 | `mb-10`, шапка без `max-w` |
| `landing/LogoMarquee.tsx` | 1 | `py-16/24/32` |
| `landing/CtaButtons.tsx` | 1 | `flushTop`, `py` вместо `pt` |

**Решение владельца:** отступы, которые уже работают у лендингов на проде.

**Риск:** вертикальный ритм лендинга «Автоматизации» отличается от того, что
согласовывали 26–28.08. Перепроверить: стыки секций «Шаг 1 / Шаг 2»,
«И еще более 300 сценариев», лого-лента — на мобилке, планшете и десктопе.

### `landing/SiteHeader.tsx` — сторона `main`

Отброшено: `data-kt-header` и инъекция `:has(> header[data-kt-header])`,
делавшая шапку липкой на **всех** лендингах.

**Решение владельца:** липкая шапка нужна одному лендингу конференции и
остаётся вариантом, а не правилом.

**Следствие:** на «Автоматизациях» шапка больше не липкая; вернулась промо-полоса.

### `landing/FeatureGrid.tsx` — объединены оба решения

Обе ветки решали «карусель на планшете» по-разному:
`main` добавил проп `slider` с собственным треком (48 строк логики),
`integration` — проп `variant: 'cards' | 'mock'` с делегированием в
`FeatureGridMock`.

Взято **и то, и другое**: хуки выполняются безусловно, ранний возврат в
`FeatureGridMock` стоит после них — правило хуков не нарушено.

**Проверить:** лендинги с `slider: true` и лендинг «Автоматизации»
(`variant: 'mock'`) — оба пути живут в одном компоненте.

### `landing/Review.tsx` — база `main` + пропы `integration`

На `main` независимо сделали то же, что и мы 28.08: счётчик, стрелки по факту
переполнения, лента во всю ширину экрана. Реализация `main` аккуратнее —
считает по содержимому трека и имеет `revx--nopager`.

Добавлено поверх со стороны `integration`: `role?` (необязательная должность),
`avatarWide`, `logoScale` — их использует спека «Автоматизаций»
(`logoScale: 0.78` у Buzzolls, `avatarWide: true` у «Технологий Надёжности»).

Правила `.otz__av--wide` и `--otz-logo-scale` пришлось **дописать руками**:
они жили в CSS-блоке `integration`, который целиком уступил стороне `main`.
Масштаб логотипа теперь множитель к высоте `main`:
`height: calc(40px * var(--otz-logo-scale, 1))`.

Стрелки — 40 px (решение владельца), у `main` было 48.

**Риск: высокий, это самый переработанный файл.** Проверить на всех лендингах
с отзывами: широкий логотип-знак, масштаб логотипа, отзыв без должности,
метрика кейса, поведение стрелок при 3 и при 6 отзывах.

### `mocks/FeatureMocksV01.tsx` — по блокам

- блок 1 → `main`: `Inter` убран;
- блоки 2–3 → `integration` (`min-height:16px`, `flex:1 1 0`, разделители),
  но **все тени приведены к `y=0`** по правилу DS с `main`.

### `mocks/CTAproduct.tsx` — сторона `main`

`main` переписал компонент: 114 строк против 75, добавлен класс `.ctp__cta`.
Отброшено со стороны `integration`: радиус 16 вместо 12 и `.ctp__btn`.

**Исправлено 28.08 после деплоя:** при разборе конфликта в файл попала версия
`integration` (75 строк) вместо `main` (114) — потерялись `.ctp__cta`, проп
`secondaryButton` и картинка по умолчанию. Оба места вызова (`CtaBanner`,
`FinalCta`) всегда передают `image` и не используют `secondaryButton`,
поэтому поломки не было. Версия `main` восстановлена отдельным коммитом.

### Данные и журналы — объединение обеих сторон

| файл | как слито |
|---|---|
| `packages/harness/src/schemas/landing-spec.ts` | 3 блока: `slider`+`flushTop` (main) и `variant` (integration); `metric`+`logoHeight` (main) и `logoScale` (integration); ограничение `.max(80)` (integration) + новые пропы карточки (main) |
| `content/illustrations/registry.json` | слит как **данные**, а не текстом: 53 + 60 записей → 61 уникальная по ключу `kind\|variant\|slug\|section\|idx`, `updatedAt` — более поздний. JSON валиден |
| `wiki/log.md` | журнал только дополняется — взяты обе стороны, 217 записей |

### Мелочи — сторона `main`

`.claude/launch.json` (форматирование),
`apps/web/public/brand/reviews/buzzolls-avatar.png` (одна и та же картинка,
пересохранена: 40058 против 40045 байт).

---

## 2. Что проверено после слияния

- Маркеров конфликта в дереве не осталось.
- Девять лендингов отдают 200: `kaiten-automation`, `kaiten-task-tracker`,
  `crm`, `kaiten-platform`, `knowledge-base`, `kaiten-cli`,
  `webinar-vnedrenie-kaiten`, `kaiten-clickup`, `kaiten-retail`.
- «Автоматизации»: 13 секций, `logoScale` 0.78 применился, широкий аватар
  129×56, стрелки 40 px, счётчик «1 / 3».
- «Таск-трекер» (сторона `main`): метрика кейса рендерится, высоты логотипов
  различаются (42 / 64 / 42 — работает `logoHeight`), слайдер фич на месте.

### Ошибки типизации — слияние их не добавило

| пакет | до слияния | после |
|---|---|---|
| `packages/ui` | 21 | 21 |
| `packages/harness` | 32 на чистом `main` | 34 |

Две новые в `harness` — не от слияния: `HeroSection.tsx` и `MediaCopy.tsx`
приехали такими со стороны `integration` (`MockVariant | "hero-screen-video"`
и `MockVariant | "none"` не входят в `MockVariant`).

Самая массовая группа, 18 ошибок в `domain-visual.ts` — секция `'feature'`
не входит в тип `Array<'hero' | 'media' | 'tab' | 'scenario'>`. Проверено:
на `1084285` до слияния ровно те же 18 вхождений. **Долг `main`, не наш.**

---

## 3. Ошибки процесса

### 3.1 Чужой `git reset --hard` посреди работы

В reflog две записи `reset: moving to HEAD`. Снесло безвозвратно:
переименование моков в `Window*`, чужие незакоммиченные правки
(`CtaBanner.tsx` — вариант `product`, `FeatureGrid.tsx`, `FeatureGridMock.tsx`,
`registry.json`, 44 строки `wiki/log.md`) и правки тем презентации.

Причина, скорее всего, — параллельная сессия: в репозитории пять worktree, среди них
`.claude/worktrees/bold-poincare-203375` на ветке
`claude/content-factory-presentation-8b3103`.

**Вывод:** пока в каталоге работают двое, любые незакоммиченные правки —
расходный материал. Коммитить чаще либо развести работу по worktree.

### 3.2 Разрушено состояние merge (моя ошибка)

Чтобы замерить эталонное число ошибок типов, я выполнил посреди слияния
`git stash` + `git checkout origin/main -- packages/…`. Это стёрло `MERGE_HEAD`,
и git перестал считать слияние незавершённым.

Разрешения уцелели в рабочем дереве; коммит пересобран через `commit-tree`
с двумя родителями, связь ветвей сохранена. Но замер стоило делать на копии,
а не в рабочем каталоге.

### 3.3 Дубль пропа при объединении (моя ошибка, поймана)

В `FeatureGrid.tsx` я склеил два описания **одного и того же** пропа
`featureTile` — получилось повторное объявление и 5 синтаксических ошибок.
Затем при вырезании дубля срезал лишние строки и сломал закрытие
`FeatureCtas`. Обе поломки поймал `tsc`, исправлены.

**Вывод:** объединять стороны конфликта построчно можно только когда это
разные сущности; одноимённые поля надо сверять по имени, а не по позиции.

### 3.4 Отклонённый пуш в `Kaiten-project`

`remote rejected … duplicateEntries: contains duplicate file entries`.
Дерево собиралось фильтром по имени папки, а `git ls-tree` печатает кириллицу
экранированной — старая запись не удалилась, в дереве оказались две записи
«Модуль «Автоматизации»». Пересобрано через `git ls-tree -z`.

---

## 4. Незакрытые хвосты

- **Переименование моков в `Window*`** — снесено `reset --hard`, не восстановлено.
  В `main` они по-прежнему `AutomationRuleTriggerMock`, `AutomationCardFlowMock`
  и т. д. Шаги известны: файлы → barrel → `MockVisual` → три энума в
  `landing-spec` → `DOMAIN_REGISTRY` → `domain-mock-matrix.md` → спека лендинга.
- **Вертикальный ритм «Автоматизаций»** после отката отступов на сторону `main`
  никто не смотрел глазами.
- **`stash@{0}`** — `On integration: epitaxy: pre-switch from integration`,
  21 файл. Происхождение неизвестно, не мой. Разобрать или удалить.
- **Прод на Vercel** собран до слияния: `content-zavod-beta.vercel.app` отдаёт
  состояние `04c9c57`.
